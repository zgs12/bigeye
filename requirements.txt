# BigEye server (server.py, auto-started by desktop)
# pip install -r requirements.txt

requests>=2.28.0
httpx>=0.27.0
numpy>=1.24.0
tokenizers>=0.15.0
onnxruntime>=1.16.0

# Optional heavy local embedding (uncomment if needed):
# sentence-transformers>=2.2.0
